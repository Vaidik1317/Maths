function voice1()
{
    const text  = "Mean selected please enter the values";
    
    
    const utterance = new  SpeechSynthesisUtterance(text);
     utterance.pitch = 5;
    
    window.speechSynthesis.speak(utterance);
}

function voice2()
{
    const text  = "Single Mean selected please enter the values";
    
    
    const utterance = new  SpeechSynthesisUtterance(text);
     utterance.pitch = 5;
    
    window.speechSynthesis.speak(utterance);
}

function voice3()
{
    const text  = "Proportion selected please enter the values";
    
    
    const utterance = new  SpeechSynthesisUtterance(text);
     utterance.pitch = 5;
    
    window.speechSynthesis.speak(utterance);
}

function voice4()
{
    const text  = "Correlation selected please enter the values";
    
    
    const utterance = new  SpeechSynthesisUtterance(text);
     utterance.pitch = 5;
    
    window.speechSynthesis.speak(utterance);
}

function voice5()
{
    const text  = "Regression selected please enter the values";
    
    
    const utterance = new  SpeechSynthesisUtterance(text);
     utterance.pitch = 5;
    
    window.speechSynthesis.speak(utterance);
}